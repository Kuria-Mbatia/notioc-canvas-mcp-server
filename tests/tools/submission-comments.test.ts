import { expect, test } from "vitest";
import * as submissionComments from "../../src/tools/submission-comments.js";

// Import all functions and types from the submission-comments module
const {
  GetSubmissionCommentsParams,
  SubmissionCommentInfo,
  CommentDetail,
  getSubmissionComments,
  // Add other exports as needed
} = submissionComments;
