import { expect, test } from "vitest";
import * as modules from "../../src/tools/modules.js";

// Import all functions and types from the modules module
const {
  listModules,
  getModuleDetails,
  ModuleListParams,
  ModuleInfo,
  // Add other exports as needed
} = modules;
