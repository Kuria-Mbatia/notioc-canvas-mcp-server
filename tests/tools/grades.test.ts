import { expect, test } from "vitest";
import * as grades from "../../src/tools/grades.js";

// Import all functions and types from the grades module
const {
  getGrades,
  getGradebookCategories,
  GradeParams,
  GradeInfo,
  // Add other exports as needed
} = grades;
