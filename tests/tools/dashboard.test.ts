import { expect, test } from "vitest";
import * as dashboard from "../../src/tools/dashboard.js";

// Import all functions and types from the dashboard module
const {
  getDashboardInfo,
  getDashboardCards,
  DashboardParams,
  DashboardInfo,
  // Add other exports as needed
} = dashboard;
