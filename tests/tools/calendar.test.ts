import { expect, test } from "vitest";
import * as calendar from "../../src/tools/calendar.js";

// Import all functions and types from the calendar module
const {
  getCalendarEvents,
  getUpcomingEvents,
  CalendarEventParams,
  CalendarEventInfo,
  // Add other exports as needed
} = calendar;
