import { expect, test } from "vitest";
import * as rubrics from "../../src/tools/rubrics.js";

// Import all functions and types from the rubrics module
const {
  getRubric,
  getRubricAssessments,
  RubricParams,
  RubricInfo,
  // Add other exports as needed
} = rubrics;
