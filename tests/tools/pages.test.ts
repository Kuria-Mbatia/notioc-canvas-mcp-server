import { expect, test } from "vitest";
import * as pages from "../../src/tools/pages.js";

// Import all functions and types from the pages module
const {
  getPageContent,
  listPages,
  PageParams,
  PageInfo,
  // Add other exports as needed
} = pages;
