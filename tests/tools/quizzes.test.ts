import { expect, test } from "vitest";
import * as quizzes from "../../src/tools/quizzes.js";

// Import all functions and types from the quizzes module
const {
  listQuizzes,
  getQuizDetails,
  QuizListParams,
  QuizInfo,
  // Add other exports as needed
} = quizzes;
