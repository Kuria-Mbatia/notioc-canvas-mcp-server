import { expect, test } from "vitest";
import * as urlProcessor from "../../src/tools/url-processor.js";

// Import all functions and types from the url-processor module
const {
  ProcessCanvasURLParams,
  ProcessedURLResult,
  processCanvasURL,
  extractFileInfoFromURL,
  // Add other exports as needed
} = urlProcessor;
