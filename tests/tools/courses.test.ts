import { expect, test } from "vitest";
import * as courses from "../../src/tools/courses.js";

// Import all functions and types from the courses module
const {
  CourseListParams,
  CourseInfo,
  listCourses,
  getCourseDetails,
  // Add other exports as needed
} = courses;
