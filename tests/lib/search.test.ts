import { expect, test } from "vitest";
import * as search from "../../src/lib/search.js";

// Import all functions and types from the search module
const {
  findBestMatch,
  searchInContent,
  fuzzySearch,
  // Add other exports as needed
} = search;
