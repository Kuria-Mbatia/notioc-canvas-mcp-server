import { expect, test } from "vitest";
import * as smallModel from "../../src/lib/small-model.js";

// Import all functions and types from the small-model module
const {
  generateSummary,
  extractKeywords,
  classifyContent,
  // Add other exports as needed
} = smallModel;
